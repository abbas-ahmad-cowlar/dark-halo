# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly initialized repository (`dark-halo`) with no application code, dependencies, or services. The repo contains only a `README.md`.

- **No build/lint/test commands** are available yet — the repository has no source code or configuration.
- **No services** need to be started.
- **No package manager** lockfile exists.
- When code is added, update this section with relevant dev environment setup, lint, test, and run instructions.
